<UserControl x:Class="ChartSettingsPanel.Sections.HudOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <GroupBox Header="HUD &amp; Overlay" Style="{StaticResource DarkGroupBox}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="HUD Position"/>
            <ComboBox Grid.Row="0" Grid.Column="1"
                      Style="{StaticResource DarkComboBox}"
                      SelectedItem="{Binding Settings.HudOverlay.HudPosition}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Text Color"/>
            <Button Grid.Row="1" Grid.Column="1"
                    Style="{StaticResource DarkButton}"
                    Content="Pick Color"
                    Command="{Binding PickHudTextColorCommand}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Font Size"/>
            <TextBox Grid.Row="2" Grid.Column="1"
                     Style="{StaticResource DarkTextBox}"
                     Text="{Binding Settings.HudOverlay.FontSize}"/>
        </Grid>
    </GroupBox>
</UserControl>
