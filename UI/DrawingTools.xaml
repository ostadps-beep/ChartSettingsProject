<UserControl x:Class="ChartSettingsPanel.Sections.DrawingTools"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <GroupBox Header="Drawing Tools" Style="{StaticResource DarkGroupBox}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Tool Color"/>
            <Button Grid.Row="0" Grid.Column="1"
                    Style="{StaticResource DarkButton}"
                    Content="Pick Color"
                    Command="{Binding PickToolColorCommand}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Tool Thickness"/>
            <Slider Grid.Row="1" Grid.Column="1"
                    Minimum="1" Maximum="6"
                    Value="{Binding Settings.DrawingTools.ToolThickness}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Snap Mode"/>
            <ComboBox Grid.Row="2" Grid.Column="1"
                      Style="{StaticResource DarkComboBox}"
                      SelectedItem="{Binding Settings.DrawingTools.SnapMode}"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Show Labels"/>
            <CheckBox Grid.Row="3" Grid.Column="1"
                      Style="{StaticResource DarkCheckBox}"
                      IsChecked="{Binding Settings.DrawingTools.ShowLabels}"/>
        </Grid>
    </GroupBox>
</UserControl>
